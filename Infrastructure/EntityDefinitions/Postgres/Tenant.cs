using Microsoft.EntityFrameworkCore;

namespace Infrastructure.EntityDefinitions.Postgres;

internal static class Tenant
{
    internal static void MapTenantDefinitions(this ModelBuilder modelBuilder)
    {
    }
}