name: meetline

services:
  postgres:
    image: postgres:18

    ports:
      - "5432:5432"

    environment:
      - POSTGRES_DB=meetline
      - POSTGRES_HOST_AUTH_METHOD=trust

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
